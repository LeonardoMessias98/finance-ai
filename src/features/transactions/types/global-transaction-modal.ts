import type { TransactionType } from "@/features/transactions/types/transaction";

export type OpenGlobalTransactionModalOptions = {
  defaultCompetencyMonth?: string;
  defaultType?: TransactionType;
};
